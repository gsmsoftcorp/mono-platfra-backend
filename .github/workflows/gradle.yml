name: Java CI with Gradle

on:
  push:
    branches: [ "insung/20240127" ]
  pull_request:
    branches: [ "insung/20240127" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'

    # tes2
    - name: test2
      run: ls
    # 폴더로 이동
    - name: Enter project
      run: cd platfra

    # test
    - name: test
      run: cd platfra
      
    # gradlew 에 권한 부여
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    # build
    - name: Build with Gradle
      run: ./gradlew clean build
